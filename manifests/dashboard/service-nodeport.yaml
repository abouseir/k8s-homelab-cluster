apiVersion: v1
kind: Service
metadata:
  labels:
    k8s-app: kubernetes-dashboard
  name: kubernetes-dashboard
  namespace: kubernetes-dashboard
spec:
  ports:
    - port: 443
      protocol: TCP
      targetPort: 8443
      nodePort: 30000  # <--- On fixe le port ici (plus facile !)
  selector:
    k8s-app: kubernetes-dashboard
  type: NodePort       # <--- C'est ici qu'on ouvre l'accès externe
sessionAffinity: None
